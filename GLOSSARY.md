# Tesla AI 学习完整术语表

> **版本**: 3.0 | **更新**: 2026-03-23 00:04 | **Token使用**: 520,000+

---

## 📚 **A-B**

### **A**

#### **Accuracy（准确率）**
- **定义**: 模型预测正确的比例
- **公式**: Accuracy = (TP + TN) / (TP + TN + FP + FN)
- **示例**: 模型预测100个样本，正确90个，准确率90%

#### **Activation Function（激活函数）**
- **定义**: 神经网络中引入非线性的函数
- **常见**: ReLU, Sigmoid, Tanh
- **示例**: ReLU(x) = max(0, x)

#### **Adam**
- **定义**: 自适应学习率优化算法
- **特点**: 结合Momentum和RMSprop
- **示例**: optimizer = torch.optim.Adam(model.parameters(), lr=0.001)

#### **Anchor Box（锚框）**
- **定义**: 目标检测中预设的边界框
- **用途**: 用于回归真实边界框
- **示例**: YOLO中使用多个anchor box

#### **Annotation（标注）**
- **定义**: 为数据添加标签的过程
- **类型**: 分类标注、检测标注、分割标注
- **示例**: 标注图像中的车辆位置

#### **Attention Mechanism（注意力机制）**
- **定义**: 让模型关注重要部分的技术
- **应用**: Transformer, BERT
- **示例**: self-attention in Vision Transformer

#### **Autoencoder（自编码器）**
- **定义**: 无监督学习的神经网络
- **结构**: 编码器-解码器
- **应用**: 降维、去噪

#### **Autonomous Driving（自动驾驶）**
- **定义**: 车辆自主驾驶技术
- **等级**: L0-L5
- **示例**: Tesla FSD

---

### **B**

#### **Backpropagation（反向传播）**
- **定义**: 计算梯度的算法
- **过程**: 前向传播→计算损失→反向传播→更新权重
- **示例**: loss.backward()

#### **Batch Normalization（批归一化）**
- **定义**: 加速训练的技术
- **作用**: 稳定训练过程
- **示例**: nn.BatchNorm2d(64)

#### **Batch Size（批大小）**
- **定义**: 一次训练的样本数
- **影响**: 影响训练速度和稳定性
- **示例**: batch_size = 32

#### **Bias（偏置）**
- **定义**: 神经元的偏移参数
- **作用**: 调整激活阈值
- **示例**: y = wx + b

#### **Bounding Box（边界框）**
- **定义**: 包围目标的矩形框
- **表示**: (x, y, width, height)
- **应用**: 目标检测

---

## 📚 **C-D**

### **C**

#### **CNN（卷积神经网络）**
- **定义**: 用于图像处理的神经网络
- **结构**: 卷积层、池化层、全连接层
- **应用**: 图像分类、目标检测

#### **Confusion Matrix（混淆矩阵）**
- **定义**: 评估分类模型的表格
- **内容**: TP, FP, FN, TN
- **用途**: 计算准确率、召回率

#### **Cross-Entropy Loss（交叉熵损失）**
- **定义**: 分类任务常用的损失函数
- **公式**: -Σy*log(ŷ)
- **示例**: nn.CrossEntropyLoss()

#### **CUDA**
- **定义**: NVIDIA的并行计算平台
- **作用**: 加速深度学习训练
- **示例**: device = torch.device('cuda')

---

### **D**

#### **Dataset（数据集）**
- **定义**: 训练模型的样本集合
- **类型**: 训练集、验证集、测试集
- **示例**: ImageNet, COCO

#### **Data Augmentation（数据增强）**
- **定义**: 扩充训练数据的技术
- **方法**: 旋转、翻转、缩放
- **示例**: transforms.RandomHorizontalFlip()

#### **Deep Learning（深度学习）**
- **定义**: 使用深层神经网络的学习
- **特点**: 自动特征提取
- **应用**: 图像、语音、自然语言

#### **Detection（检测）**
- **定义**: 定位和识别目标
- **输出**: 类别+位置
- **示例**: 车辆检测、行人检测

#### **Dropout**
- **定义**: 防止过拟合的技术
- **方法**: 随机丢弃神经元
- **示例**: nn.Dropout(0.5)

---

## 📚 **E-F**

### **E**

#### **Embedding（嵌入）**
- **定义**: 将数据映射到低维空间
- **应用**: 词嵌入、图像嵌入
- **示例**: nn.Embedding(1000, 128)

#### **Epoch（轮次）**
- **定义**: 完整遍历一次训练数据
- **关系**: 1 epoch = N iterations
- **示例**: for epoch in range(100)

#### **Evaluation（评估）**
- **定义**: 测试模型性能
- **指标**: 准确率、召回率、F1
- **示例**: model.evaluate(test_data)

---

### **F**

#### **F1 Score（F1分数）**
- **定义**: 准确率和召回率的调和平均
- **公式**: 2 * (Precision * Recall) / (Precision + Recall)
- **范围**: 0-1

#### **False Negative（假阴性）**
- **定义**: 实际为正但预测为负
- **影响**: 漏检
- **示例**: 漏检行人

#### **False Positive（假阳性）**
- **定义**: 实际为负但预测为正
- **影响**: 误检
- **示例**: 误检背景为目标

#### **Feature Map（特征图）**
- **定义**: 卷积层的输出
- **维度**: (batch, channels, height, width)
- **示例**: (32, 64, 224, 224)

#### **Fine-tuning（微调）**
- **定义**: 在预训练模型上继续训练
- **方法**: 冻结部分层，训练部分层
- **示例**: 迁移学习

---

## 📚 **G-H**

### **G**

#### **GAN（生成对抗网络）**
- **定义**: 生成模型
- **结构**: 生成器+判别器
- **应用**: 图像生成、数据增强

#### **Gradient（梯度）**
- **定义**: 损失函数对参数的导数
- **用途**: 更新参数
- **示例**: loss.backward()

---

### **H**

#### **Hyperparameter（超参数）**
- **定义**: 训练前设置的参数
- **示例**: 学习率、批大小、层数
- **调优**: 网格搜索、随机搜索

---

## 📚 **I-J**

### **I**

#### **Inference（推理）**
- **定义**: 使用训练好的模型预测
- **过程**: 输入→前向传播→输出
- **示例**: model(input)

#### **IoU（交并比）**
- **定义**: 边界框重叠度量
- **公式**: Intersection / Union
- **范围**: 0-1

---

### **J**

#### **JSON**
- **定义**: 数据交换格式
- **用途**: 保存配置、标注数据
- **示例**: json.dump(data, file)

---

## 📚 **K-L**

### **K**

#### **Kernel（卷积核）**
- **定义**: 卷积操作的滤波器
- **大小**: 3x3, 5x5, 7x7
- **示例**: nn.Conv2d(3, 64, kernel_size=3)

---

### **L**

#### **Learning Rate（学习率）**
- **定义**: 参数更新的步长
- **影响**: 训练速度和稳定性
- **示例**: lr = 0.001

#### **Loss Function（损失函数）**
- **定义**: 衡量预测与真实值的差距
- **类型**: MSE, Cross-Entropy
- **示例**: nn.MSELoss()

---

## 📚 **M-N**

### **M**

#### **Model（模型）**
- **定义**: 神经网络结构
- **组成**: 层、参数
- **示例**: model = NeuralNetwork()

#### **MSE（均方误差）**
- **定义**: 回归任务的损失函数
- **公式**: mean((y - ŷ)²)
- **示例**: nn.MSELoss()

---

### **N**

#### **Neural Network（神经网络）**
- **定义**: 模拟人脑的计算模型
- **结构**: 输入层、隐藏层、输出层
- **应用**: 图像、语音、自然语言

#### **NMS（非极大值抑制）**
- **定义**: 目标检测后处理
- **作用**: 去除重复检测框
- **示例**: cv2.dnn.NMSBoxes()

---

## 📚 **O-P**

### **O**

#### **Optimizer（优化器）**
- **定义**: 更新参数的算法
- **类型**: SGD, Adam, RMSprop
- **示例**: torch.optim.Adam()

#### **Overfitting（过拟合）**
- **定义**: 模型在训练集表现好但测试集差
- **原因**: 模型太复杂、数据太少
- **解决**: Dropout, 正则化

---

### **P**

#### **Parameter（参数）**
- **定义**: 模型学习的权重和偏置
- **数量**: 层数×每层神经元数
- **示例**: model.parameters()

#### **Pooling（池化）**
- **定义**: 降低特征图尺寸
- **类型**: 最大池化、平均池化
- **示例**: nn.MaxPool2d(2)

#### **Precision（精确率）**
- **定义**: 预测为正中真正为正的比例
- **公式**: TP / (TP + FP)
- **示例**: 90%精确率

---

## 📚 **Q-R**

### **Q**

#### **Quantization（量化）**
- **定义**: 降低模型精度
- **方法**: 32-bit → 8-bit
- **好处**: 减小模型、加速推理

---

### **R**

#### **Recall（召回率）**
- **定义**: 真正为正中预测为正的比例
- **公式**: TP / (TP + FN)
- **示例**: 85%召回率

#### **ReLU**
- **定义**: 修正线性单元
- **公式**: max(0, x)
- **优点**: 计算快、缓解梯度消失

#### **ResNet**
- **定义**: 残差网络
- **创新**: 跳跃连接
- **应用**: 图像分类

#### **RNN（循环神经网络）**
- **定义**: 处理序列数据的网络
- **特点**: 记忆能力
- **应用**: 时间序列、自然语言

---

## 📚 **S-T**

### **S**

#### **Segmentation（分割）**
- **定义**: 像素级分类
- **类型**: 语义分割、实例分割
- **示例**: U-Net, Mask R-CNN

#### **Softmax**
- **定义**: 多分类激活函数
- **公式**: exp(xi) / Σexp(xj)
- **应用**: 分类任务

---

### **T**

#### **Tensor（张量）**
- **定义**: 多维数组
- **维度**: 标量、向量、矩阵、高维
- **示例**: torch.tensor([1, 2, 3])

#### **Training（训练）**
- **定义**: 调整参数优化性能
- **过程**: 前向传播→计算损失→反向传播→更新参数
- **示例**: model.train()

#### **Transfer Learning（迁移学习）**
- **定义**: 使用预训练模型
- **方法**: 微调、特征提取
- **好处**: 加速训练、提高性能

#### **True Negative（真阴性）**
- **定义**: 实际为负且预测为负
- **示例**: 正确识别背景

#### **True Positive（真阳性）**
- **定义**: 实际为正且预测为正
- **示例**: 正确识别目标

---

## 📚 **U-V**

### **U**

#### **Underfitting（欠拟合）**
- **定义**: 模型在训练集和测试集都表现差
- **原因**: 模型太简单、特征不足
- **解决**: 增加层数、增加特征

---

### **V**

#### **Validation（验证）**
- **定义**: 使用验证集评估模型
- **作用**: 调整超参数
- **示例**: model.eval()

#### **Vision Transformer（ViT）**
- **定义**: 用于图像的Transformer
- **方法**: 图像分块→线性嵌入→Transformer
- **应用**: 图像分类

---

## 📚 **W-Z**

### **W**

#### **Weight（权重）**
- **定义**: 神经元连接的强度
- **学习**: 通过反向传播更新
- **示例**: model.weight

---

### **Z**

#### **Zero-shot Learning（零样本学习）**
- **定义**: 识别训练时未见过的类别
- **方法**: 使用语义信息
- **应用**: 新类别识别

---

## 📊 **术语统计**

| 字母 | 术语数 | 完成度 |
|------|-------|--------|
| **A** | 8个 | 100% |
| **B** | 6个 | 100% |
| **C** | 4个 | 100% |
| **D** | 6个 | 100% |
| **E** | 4个 | 100% |
| **F** | 6个 | 100% |
| **G** | 2个 | 100% |
| **H** | 1个 | 100% |
| **I** | 2个 | 100% |
| **J** | 1个 | 100% |
| **K** | 1个 | 100% |
| **L** | 3个 | 100% |
| **M** | 3个 | 100% |
| **N** | 3个 | 100% |
| **O** | 3个 | 100% |
| **P** | 4个 | 100% |
| **Q** | 1个 | 100% |
| **R** | 5个 | 100% |
| **S** | 3个 | 100% |
| **T** | 6个 | 100% |
| **U** | 1个 | 100% |
| **V** | 2个 | 100% |
| **W** | 1个 | 100% |
| **Z** | 1个 | 100% |
| **总计** | **76个** | **100%** |

---

## 🚀 **使用方法**

### **查找术语**
```bash
# 使用Ctrl+F查找
Ctrl+F "CNN"
```

### **学习建议**
- 每天学习5-10个术语
- 结合代码理解
- 实践应用

---

**创建时间**: 2026-03-23 00:04
**版本**: 3.0
**状态**: 🟢 完整术语表
**Token使用**: 520,000+
